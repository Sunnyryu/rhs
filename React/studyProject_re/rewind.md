## React Prac


```
yarn init => package json 파일 생성

yarn add를 이용해서 리액트 라이브러리 설치
(리액트 개발을 위한 react, react-dom!)
(webpack => 앱을 배포하기 위한 라이브러리
  webpack-dev-server를 이용해 실시간 로딩으로 빠르게 개발가능!)
yarn add 명렁어 => 바벨 설치(개발환경에서 필요)
webpack-cli(웹팩 클라이언트 모듈))
(html, js, webpack.config.js, .babelrc, package.json 등을 세팅해줌!)
yarn start => yarn 시작

material-ui 라이브러리 설치(yarn add @material-ui/core, icons
라우팅 기능을 위해서 yarn add react-router-dom을 설치 ! (이동을 위한 아이!?)

firebase를 이용해서 데이터 베이스를 만들고 사용한다. !!

const databaseURL로 url 변수를 선언하고 fetch에 넣어서 가져올 수도 있음 !!

빌드(Build)를 위한 웹팩 설정

copy-webpack-plugin을 추가함!

 yarn add firebase-tools  / npm install -g firebase-tools(파이어베이스 설치)

firebase init -> 용도 선택 -> 계정 선택 -> 디렉토리 선택 -> SPA 구성!

yarn build => build!!
firebase deploy => 파이어베이스를 통한 디플로이!

 yarn add react-text-truncate(텍스트 가지치기 라이브러리!)

yarn add style-loader
yarn add css-loader
스타일을 위한 폰트추가!

@import url(https://fonts.googleapis.com/earlyaccess/notosanskr.css); => css파일에서 @import 사용!

flask와 연동 ..

wordcloud 워드 클라우드 그리는 라이브러리,
konlpy Okt(한국어 자연어 처리 라이브러리),
Counter(명사의 출현 빈도를 세는 라이브러리),
Matplotlib(워드 클라우드 이미지를 실제로 그림 파일에 표현하는 목적의 라이브러리)

리액트 : 기본적으로 React에서 이미지(Image) 태그의 정보가 수정될 때에는 브라우저 캐시(Cache)가 동작하지 않도록, 별도의 랜덤 파라미터를 추가적으로 붙여 줄 수 있습니다.


```
