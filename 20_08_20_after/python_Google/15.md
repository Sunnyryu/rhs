# 머신러닝(클러스터링 1)

```
ML 애플리케이션에 대한 클러스터링을 정의

항목을 그룹화하는 방법.. 

라벨이 지정되지 않은 것들의 그룹화!?

라벨이 지정되지 않은 경우 클러스터링은 무감독 시스템에 의존함!
라벨이 지정되면 클러스터링이 되유!

유사한 예를 그룹화하려면 먼저 유사한 예를 찾아야함! / 예를 결합하여 유사성 측정 -> 데이터를 유사성 측도라고 하는 메트릭에 피쳐링, 하나또는 두개의 형상에 의해 정의

클러스터링을 사용하는 예시(시장세분화, 소셜 네트워크 분석, 검색 결과 그룹화, 의학 영상 촬영, 영상 분할, 이상감지)

클러스터링 후 각 클러스터에는 클러스터 ID라는 번호가 할당 -> 피쳐 세트를 클러스터 ID로 압축가능!

학습 시스템은 클러스터 ID를 사용하여 시스템 프로세스를 단순화할 수 있음
ex)
일반화

재밌는 뮤비를 보고 재미 없는 뮤비는 클러스터링 할 수 있음 (뮤비 권장사항을 개선)

데이터 압축 (전체 피쳐 데이터 세트 입력 데이터의 복잡성을 줄이면 ML 모델이 됌!)
ex) 동영상에 위치 및 시간에 대한 뷰어데이터, 타임스탬프, 사용자 id가 있는 주석 데이터, 동영상 태그가 있다면 클러스터링을 수행 후 세트 기능을 다른 것으로 대체가 가능함!

프라이버시 보존 ) 사용자를 클러스터링 후에 사용자 데이터를 연결하여 개인 정보 본존 가능!
(사용자를 연결할 수 없도록 하려면 특정 사용자가 있는 데이터, 클러스터는 충분한 수의 사용자를 그룹화해야함)

클러스터링 알고리즘

클러스터링 알고리즘을 선택할 때 알고리즘의 사용 여부를 고려, 데이터 집합으로 확장 
but) 모든 클러스터링 알고리즘이 효율적으로 확장되지는 않음

클러스터링 유형
중심 기반 클러스터링 -> 데이터를 비계층적 클러스터로 구성 (효율적이고, 효과적이며, 단순한 클러스터링) / k-평균이 가장 많고, 효율적이지만 초기조건 및 특이치에 민감함!

밀도기반 클러스터링 -> 예시 밀도가 높은 영역을 클러스터로 연결 / 특이치 할당 x

배포 기반 클러스터링 -> 가우스 분포도 사용, 알고리즘 클러스터 데이터 3개 가우스 분포. 분포의 중심으로부터의 거리가 증가함에 따라, 분포의 중심으로부터의 거리가 증가, 점이 분포에 속할 확률은 감소
분포 유형을 모른다면 다른 알고리즘을 사용해야 함!

계층적 클러스터링 (클러스터 트리 생성 , 클러스터 수를 선택할 수 있음! )
```

```
클러스터링 워크플로우

데이터 준비 -> 유사성 메트릭 생성 -> 클러스터링 알고리즘 실행 -> 결과해석 클러스터링 조정 !

데이터 준비 : (ML 문제와 마찬가지로 형상 데이터를 정규화, 스케일링 및 변환 / 클러스터링 하는 동안 준비된 데이터 추가 확인 / 유사성 정확히 계산 가능! ) 
유사성 메트릭 생성 : 클러스터링 알고리즘이 데이터를 그룹화하려면 먼저 얼마나 유사한지 알아야함 (유사성 수량화하여 유사도 측정법 유사성 메트릭 생성)

클러스터링 알고리즘 생성 : (클러스터링 알고리즘은 유사성 메트릭 사용/ 데이터 클러스터링 / k-means 초점 맞춤)
결과 해석 및 조정 ( 클러스터링 출력의 품질을 확인하는 것이 반복적이고 탐색적) /  결과를 개선하려면 이전 단계를 반복적으로 실험
```

```
데이터 준비 

군집화에서는 두 예제 간의 유사성을 결합하여 계산 / 모든 형상 데이터를 숫자 값으로 변환 

기능 데이터에는 데이터 척도가 같아야함 

데이터 정규화 
여러 형상에 대한 데이터를 정규화하여 동일한 척도로 변환할 수 있음 
자료 특히 정상화는 가장 처리에 적합 / 정상화가 있는 경우와 없는 경우의 유사성을 살펴볼 필요가 있음!

정상화 :(실제 값의 범위를 표준 범위로 변환하는 프로세스 일반적으로 -1에서 +1 또는 0에서 1 사이의 값) / (뺄셈과 나눗셈을 통해 이러한 값을 -1 ~ +1 범위로 정규화할 수 있음)


공통 데이터 분포, 가우스 분포분포의 에 비해 정량화, 정규화에는 계산하는 데 필요한 데이터가 상당히 적음 
데이터를 정규화한 후 유사도를 더 정확하게 계산할 수 있음

데이터에 가우스 분포가 있거나 데이터 세트에는 정량형을 만들 수 있는 데이터 부족

로그 변환 사용 (로그 변환을 사용하여 전원 관리 배포를 처리)
수량 사용 (정규화 및 로그는 특정 데이터 분포를 변환 

데이터 정량형으로 변환 
간격의 수 결정 -> 구간 정의, 각 구간이 동일한 수  -> 각 예를 해당 간격의 인덱스로 바꿈 -> 스케일링을 통해 인덱스를 다른 피쳐 데이터와 동일한 범위로 가져오기 인덱스 값은 [0,1]임

데이터를 정량형으로 변환한 후에는 두 데이터 간의 유사성 예는 그 둘 사이의 예시 수와 반비례한다. 
분량은 데이터를 변환하는 데 가장 적합한 기본 옵션 (정량형 데이터, 많은 자료가 필요)
```

```
수동 유사성 측정

유사성 계산을 위해서는 예를 결합해야함! / 데이터를 숫자 값으로 변환한다거나?
RMSE(루트 평균 제곱 오차)를 사용하여 데이터를 결합

ex) 신발의 가격과 크기를 이용해 한다고 해보자

크기 조정 / 가격 조정 / 크기의 차이를 찾기 / 가격의 차이를 찾기 / RMSE를 찾음!

직관적으로 형상 데이터가 증가시 유사도는 증가 / 측정된 유사성은 감소! (유사성은 1 - RMSE)

범주형 데이터를 가지고 있다면 고정된 장르의 집합 공통값의 비율을 사용하여 유사도를 계산하면 좋음!

일반적으로 유사성 측정에 직접 대응해야함! / 메트릭이 그렇지 않으면 메트릭을 인코딩이 안됨! / 
```

```
수동 유사성 측정 운동
EX) 
가격 (양의정수) / 크기 (제곱미터 단위의 양의 부등 소수점 ) / 우편번호(정수) / 침실수(정수) / 주택의 종류 (싱글 패밀리 텍스트이 값 -> 다세대, 아파트, 콘도) / 차고지(0/1 -> 아니요/예) , 색(다중값 범주형!)

전처리 -> 가격,크기, 숫자 기능을 사전 처리 / 침실의 개수, 우편번호, 이러한 기능 수행 
EX) 

데이터가 바이모델 배분해야 할 떄 어떻게 해야할까.. -> 데이터 정량형을 생성하고 [0,1]로 확장함!

크기 데이터를 처리하는 방법 -> 크기가 파워, 포아송, 가우스 분포를 따르는지 확인 / 변환을 기록하고 [0,1]로 확장 하기 / 정분위수를 만들고 [0,1]까지 스케일링 / 가우스: 정규화하고 [0,1]로 확장

침실 수에 대한 데이터를 처리하는 방법을 설명 -> 침실 수는 분배 상태를 확인하십시오. 대부분의 경우 특이치를 잘라내고 [0.1]로 확장하는 것이 적절 / 파워로 분포를 찾으면 로그 변환이 필요할 수 있음

우편번호는 경도 및 위도로 변환하기!

형상별 유사도 계산
이제 피쳐당 유사성을 계산해야 할 때입니다. 숫자 형상의 경우 당신은 단지 차이점을 찾을 뿐이다. 하우스의 경우처럼 2진 형상의 경우 차고, 당신은 또한 0 또는 1을 얻을 수 있는 차이점을 찾을 수 있음

다중값 가지는 것 -> 색채 / 유사성 측정값을 사용하여 값을 계산 -> 자카드 유사도

전체 유사도 계산
모든 형상의 유사도를 수치적으로 계산했다. 그러나 그 클러스터링 알고리즘에는 클러스터 하우스와 전체적인 유사성이 필요,  주택 한 쌍의 전체 유사도를 1개당 합쳐서 계산 / RMSE를 사용하여 유사성 피쳐링함! 

수동 유사도 측정 한계
데이터가 복잡해지면 점점 더 어려워 짐, 데이터를 처리하고 결합하여 공통성을 정확하게 측정, 의미론적으로 의미 있는 방법 색상 데이터를 고려, 데이터 결합과 관련하여, 우리는 단지 가중치를 부여 


```